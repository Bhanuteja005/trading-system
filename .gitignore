# ── Secrets — NEVER commit ──────────────────────────────────
**/.env
*.env
.flaskenv
**/local_config.py
**/*.key
**/db/*.db
**/*.db
**/*.db-shm
**/*.db-wal
**/*.db-journal
**/*.duckdb
**/*.sqlite

# Disabled nested git repos (backups from vendoring)
**/_git_backup/

# ── Trading data / runtime logs ─────────────────────────────
data/
**/trade_log.json
**/trades.json
**/*.log
**/*.jsonl
packages/tradingview-mcp/screenshots/

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/

# ── Node ────────────────────────────────────────────────────
node_modules/

# ── Editor / OS ─────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# Structural reference repo (separate git repo, has its own .env) — never commit
monorepo/
.env
